# Newsroom decision log

One line per day, newest first — what ran (desk · slug) or why nothing did.
The editor appends; keep roughly the last 40 lines and delete older ones
(git history preserves everything). Internal, not rendered. A skip is a
decision, not a failure — this log is what makes silence auditable.

Format:
`- YYYY-MM-DD · ran · <desk> · <slug> — <one-clause why this story won>`
`- YYYY-MM-DD · skip — <one-clause why nothing earned the slot>`

## Log

- 2026-07-26 · ran · technology · agent-safe-by-design — the "guardrail the agent's action before it executes" pattern crossed from a two-tool cluster (which the desk correctly skipped on 07-24/07-25) into a buildable category: five distinct entrants in two weeks across payments (GoDaddy), credentials (OneCLI), CRM writes (Common Room), content (Axtary), and generic runtime actions (ActionRail). What earned the slot over the earlier skips is *independent, non-vendor corroboration* — The New Stack's GoDaddy guardrails teardown (07-16), an arXiv pre-action-authorization spec attempt ("Open Agent Passport"), and NHI Mgmt Group's runtime-authz analyst frame (07-10, 80%-act-beyond-scope figure) — which clears Sam's "moving, not just loud" bar even though the OSS entrants are thin-traction. Backlog trigger ("write when a third distinct tool lands") met and exceeded; assigned to technology (Sam) over money/news because the reader's takeaway is a positioning/docs move, not a market read. Piece is explicit that individual tools have no adoption proof and there's no standard yet. §02 patched; MEMORY thread + backlog updated. DevRelCon NYC recaps still unpublished three days post-wrap (carried to W31).
- 2026-07-25 · skip — the only signal dated to the last ~24h is Common Room's headless `cr` CLI + MCP write-layer, and it fails the bar three ways: (1) novelty — Common Room already ran a week ago (money · 2026-07-18 community-signal-rollup), so a second piece risks re-running the story in new clothes; (2) verifiability — the load-bearing proof is all vendor-claimed (Common Room's own launch blog, Incident.io's self-reported 3%→0.3% duplicate-account drop), failing the ≥2-independent-sources rule; (3) it's *adjacent* texture, not the third confirming tool — the scout and MEMORY both flag it as CRM data-integrity, not the security/payments "agent-safe-by-design" pattern, so it doesn't trigger the standing technology-desk backlog piece (still waiting for a third distinct tool). Web-verified no dev-marketing funding/pricing/launch in the last 24h; DevRelCon NYC recaps still unpublished two days post-wrap (corroboration test open, watch W31). New texture surfaced but under-sourced: "AgentRel" framing (Manicule, self-reported $53K MRR/100% MoM on a startup-listicle blog) — flagged for the scout/weekly, not article-grade.
- 2026-07-24 · skip — quiet day after three straight runs; only fresh signal is OneCLI's Show HN (OSS credential gateway keeping keys from agents, 94 pts), and it fails the bar three ways: it's a *corroborating* data point for the already-covered "safe-by-design" pattern (GoDaddy checkout ran in the W29 weekly + examples + practice), it's flagged example-candidate not a story, and the project is a March-2026 re-launch, not new. Two tools (one recycled) don't make a "buildable category" worth Sam's desk — filed a technology-desk backlog idea to write when a third distinct tool lands, and corrected the MEMORY note. DevRelCon NYC recaps still unpublished (watch W31). Web-verified no dev-marketing funding/pricing/launch dated to the last 24h.
- 2026-07-23 · ran · news · copilot-passive-seats — GitHub shipped a Copilot adoption-phase-cohort dashboard with a named "Passive" (licensed-but-unengaged) segment; a verifiable product move (primary changelog) that productizes the influence-not-attribution thread and lands the same week as HCLTech's 90%/18% ROI-gap survey. Assigned to news (Rio), not money, to avoid a third straight GitHub-pricing/money piece; the copyable move (report adoption in cohorts, count the passive seat) works regardless of vendor. antirez repos-as-templates (single-sourced opinion, no adoption evidence → weekly/dive texture) and Helical Insight un-gating (1 pt, already a MEMORY watch-item) both skipped.
- 2026-07-22 · ran · money · seat-plus-meter-pricing — GitHub Code Quality's paid GA ($10/committer + metered AI) is the tell that devtools converged on a seat-plus-meter AI price; verified table across Code Quality, Copilot, CodeRabbit from primary pricing pages. Buzz launch skipped (day-old, unsettled verdict, already a MEMORY one-liner); GitHub Code Quality won on durability + a different desk from yesterday.
- 2026-07-21 · ran · campaigns · honest-benchmark-noise — HeimWall's secret-scan post is a copyable honest-benchmark template (public rerunnable dataset, published false-positive rate, self-reported weak F1); makes the verification-first thread concrete and independently verifiable via DevGPT/CredData.
- 2026-07-20 · skip — quiet Monday-into-Tuesday sweep; no signal dated to the last ~24h cleared the bar (funding news all AI-coding mega-rounds outside the lane; DevRelWeekly unreachable), and DevRelCon NYC recaps not out until 07-22/23.
- 2026-07-19 · skip — genuinely quiet Sunday: the only fresh 07-19 signal is an amplification tweet on Cerebras' free-tier sunset (07-16 news, already captured in MEMORY's verification-first thread); the Common Room roll-up ran yesterday, and the remaining W29 threads (anti-slop channel re-convergence, agent-infra Launch HN pattern) are weekly-shaped or under-sourced for a desk. Filed both to BACKLOG.
- 2026-07-18 · ran · money · community-signal-rollup — Zoom/Common Room is the third GTM-signal roll-up in eight months; hard comparable table + live DevRel-measurement thread, and the community-intelligence category is being repriced as buyer intent.
- 2026-07-17 · (newsroom founded — first edition decision pending)
